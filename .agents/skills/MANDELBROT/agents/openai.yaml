interface:
  display_name: "MANDELBROT"
  short_description: "Repo-specific patterns and workflows for MANDELBROT"
  default_prompt: "Use the MANDELBROT repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true